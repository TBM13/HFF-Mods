### This mod adds a command to the console to change the jump impulse to any value.
___

#### Usage:

`jumpimpulse [value]` or `jumpimp [value]` or `jumpmod [value]`

(replace `[value]` with the value you want)
